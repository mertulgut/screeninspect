{
  "name": "@screeninspect/mcp-server",
  "version": "1.0.0",
  "description": "ScreenInspect MCP Server — on-demand screen region capture for LLMs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "screeninspect-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["mcp", "screen-capture", "macos", "llm", "tool"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "tsx": "^4.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
